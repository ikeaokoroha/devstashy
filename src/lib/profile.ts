// Typed into the delete dialog to confirm, so the button alone can't wipe an
// account. Lives here rather than in the action: a "use server" module can only
// export async functions.
export const DELETE_CONFIRMATION = "DELETE";
