## History

### Initial Next.js and Tailwind setup

Scaffolded the project with Next.js 16.3.4 / React 19.2.8 (TypeScript, App Router) and
Tailwind CSS v4. Stripped the `create-next-app` boilerplate, set up the root layout with
Geist fonts, and added `CLAUDE.md` and the `context/` docs.
